# --- scratch, never evidence -------------------------------------------------
__pycache__/
*.pyc
.DS_Store

# klayout-tools working directories. Every `klt` verb writes its scratch into
# a `.klt/` next to the request document; the durable artifacts are the JSON
# reports copied under `sim/` as append-only evidence records.
.klt/

# cocotb / Verilator build trees (tb/run_tb.py and klt both rebuild these from
# the committed sources in seconds -- nothing here is evidence).
tb/sim_build/
tb/results.xml
bench/build/
sim_build/
results.xml

# ECP5 build products. `cd fpga && make build` regenerates all of them from
# the committed sources; the measured utilisation/Fmax figures live in the
# README and in sim/ records, not in a committed log.
fpga/bitstream.bit
fpga/bitstream.config
fpga/bitstream.json
fpga/synth.log
fpga/pnr.log

# Machine-local environment shim. Cold-start setup is documented in the
# README; an env.sh naming absolute paths on one host is not committed.
env.sh
# >>> loom-managed (do not edit) >>>
# Loom runtime state (don't commit these)
.loom-in-use
.loom-checkpoint
.loom-managed
.no-changes-needed
.snapshots/
.loom/.daemon.pid
.loom/.daemon.log
.loom/daemon.sock
.loom/daemon-loop.pid
.loom/daemon-metrics.json
.loom/loom-source-path
.loom/spawn-loop-state.json
.loom/spawn-loop.pid
.loom/stop-spawn-loop
.loom/issue-failures.json
.loom/interventions/
.loom/worktrees/
.loom/worktrees-local/
.claude/worktrees/
.loom/state.json
.loom/mcp-command.json
.loom/activity.db
.loom/claims/
.loom/locks/
.loom/signals/
.loom/status/
.loom/retry-state/
.loom/exit-codes/
.loom/sweep-checkpoint/
.loom/sweep-run/
.loom/stats/
.loom/diagnostics/
.loom/guide-docs-state.json
.loom/metrics_state.json
.loom/manifest.json
.loom/stuck-config.json
.loom/metrics/
.loom/usage-cache.json
.loom/claude-config/
.loom/tokens/
.loom/accounts.env
.loom/account-health.json
.loom/account-health.lock
.loom/CANARY
.loom/*.log
.loom/*.sock
.loom/*.tmp
.loom/*.bak
.loom/.install.lock
.loom/logs/
.loom/.resync-in-progress
.loom/gh-config/
.loom/gh-config-by-owner/
# <<< loom-managed <<<
